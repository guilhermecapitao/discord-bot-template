import { SendMessageCommand } from './SendMessageCommand';

export const sendMessageCommand = new SendMessageCommand({
  name: 'send-message-with-bot',
  description: 'Envie a mensagem de boas vindas com o bot.',
});
