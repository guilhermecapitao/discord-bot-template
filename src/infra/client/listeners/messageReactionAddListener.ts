import { MessageReaction } from 'discord.js';

export async function messageReactionAddListener(
  reaction: MessageReaction,
  // user: User | PartialUser,
): Promise<void> {
  // eslint-disable-next-line no-console
  // console.log(reaction.emoji.name);
}
